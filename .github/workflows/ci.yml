name: CI

on:
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v6

      - name: Test
        run: swift test

      - name: Build release
        run: swift build -c release
